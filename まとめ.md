# プロンプト定型文作成ツール

## 作業概要
用途を選んで内容を入れるだけで、AIの返答が毎回安定する“型”のプロンプトを自動生成する単体HTMLツール。当初は「役割・依頼・出力条件を選ぶだけの簡易フォーム」として作成したが、対話を重ねる中で要望が段階的に高度化し、最終的には（1）目的ベースの依頼文と用途ごとの品質基準を持つ生成エンジン、（2）操作とプレビューが色で対応する視覚設計のUI、（3）AI/人間どちらが読んでも作業を書く相手に合わせてモデルを選べる「開発ワークフロー」プリセット、（4）文章に定型ルールを強制するオプション機能（R80ルール）まで備えるツールに育った。改修はv2〜v7の7段階で行い、都度このファイルに追記する形で経緯を残している。

## 使ったインプット
なし（すべて対話内の要望のみで開発。唯一の例外はR80ルール実装時にユーザーが提示した「二文構成・80字以内・接続詞指定」の元資料で、そこからルールのテキストを抽出して機能化した）

## アウトプット一覧
- `プロンプトビルダー.html` — 単体で動作するプロンプト生成ツール本体。外部ライブラリ・サーバー不要で、ブラウザで開くだけで使用できる。11種の用途プリセット、トーン・分量スライダー、AIっぽさ除去スイッチ、R80ルールトグル、推奨モデル表示、整形/テキストの2ビュー切替を備える。
- `まとめ.md` — 本ファイル。v2以降の全改修の経緯・目的・変更内容を記録した開発ログ。

## 各バージョンでの変更の詳細

### v13 の変更（分量デフォルトの見直し）
ユーザーからの指摘：「分量指定を実際に使うのは要約・文章作成・メール作成くらいで、それ以外は基本『徹底的』にしておいたほうが良いのでは」。妥当性を検討し、大筋で同意した上で2つの例外を提案・実装した。
- **徹底的（レベル4）に変更**：コード／UI・デザイン／アイデア出し／添削・校正／マニュアル作成／評価・採点／仕上げ／自由／① 設計・構造／③ 重点レビュー
- **現状維持（変更なし）**：要約（0）／文章作成（3）／メール作成（1）／翻訳（2、ユーザーの対象外指定にはないが、分量が原文の長さに従うべきタスクのため据え置き）
- **例外として意図的に据え置いた2件**：
  - **② 実装・反復**（短め=1のまま）：この工程は設計上「仕様どおり最小限、勝手に広げない」ことが目的（[[v7]]で導入したモデル使い分けサンドイッチの中間工程）。徹底的にすると「エッジケースやエラー処理まで網羅し、規模を惜しまず作り込む」という指示になり、②の役割（安く速く・スコープを広げない）と矛盾するため据え置いた
  - **作業まとめ**（短め=1のまま）：品質基準に「冗長な経過説明は削り、結果と根拠だけ残す」と明記しており、徹底的（字数上限なし・深く掘り下げる）は簡潔さの要求と衝突するため据え置いた
- ブラウザで全16プリセットの `state.length` を出力させて意図どおりの値になっていることを確認済み。GitHubへコミット・プッシュ済み。

### v12 の修正（コード系プリセットの分量指示バグ）
ユーザーからの指摘：「開発ワークフローの実装（②実装・反復）で分量が300字程度になっているのはおかしい」。原因を確認したところ、分量スライダーは常に文字数（「〜字程度」）を出力する作りで、これは文章向けの指標であり、コード生成には無意味な指示になっていた（コード量は文字数ではなく実装範囲・作り込みの度合いで測るべきもの）。影響していたのは「コード」「②実装・反復」「UI・デザイン」の3プリセット（いずれも成果物がコード）。
- **修正内容**：文字数ベースの `LENGTH_LEVELS` とは別に、実装規模で表現する `CODE_LENGTH_LEVELS`（最小限の差分〜エッジケース・エラー処理まで網羅、の5段階）を新設。各プリセットに `lengthKind:"code"` フラグを追加し、生成時にプリセットの種類に応じて参照する配列を切り替えるようにした。
- **対象外の確認**：「① 設計・構造」（出力は設計文書でコードは書かない仕様のため）と通常の文章系プリセットは文字数ベースのままで妥当と判断し、変更していない。
- ブラウザで実行し、コード系3プリセットが実装規模の指示文に、それ以外（要約・設計等）が引き続き文字数の指示文になることを確認済み。GitHubへコミット・プッシュ済み。

### v11 の追加（マニュアル作成プリセット）
- **📘 マニュアル作成プリセットを新設**（定番タスク、Haiku/Sonnet・工数小）。手順を1ステップ1操作の粒度に分割し、前提条件・準備物を先に明記、つまずきやすい箇所に注意点や対処を添え、専門用語は初出で説明する設計。出力形式は「手順ステップ」固定。定番タスクは合計13種（要約・文章作成・メール・翻訳・コード・UI/デザイン・アイデア出し・添削・マニュアル作成・評価採点・仕上げ・作業まとめ・自由）＋開発ワークフロー3種の計16プリセットになった。

### v10 の追加（評価・採点プリセット／無駄な忖度トグル）
- **📊 評価・採点プリセットを新設**（定番タスク、Sonnet・工数中）。単純な採点ではなく「①AIが対象に合う5つの評価観点を提案→②ユーザーが承認・修正指示→③確定した観点で10点満点採点」の2段階フローを依頼文と品質基準の両方で強制する設計にした。品質基準の1番目に「最初の応答では採点をせず、観点と選定理由だけ示して確認を求めて止まる」を置き、進め方にも「不明点は先に質問」を重ねて、確認をスキップさせない二重の縛りにしている。出力形式は表、5観点の合計・平均に加えて最も改善余地が大きい観点を1つ明示させる。
- **AIっぽさ除去に「無駄な忖度」を追加**（8→9項目）。「恐れ入りますが」「もし可能であれば」等のへりくだった前置きや、指摘・反論を和らげる遠回しな言い方をやめ、率直に言い切らせるルール。他の項目と同じくON/OFFのトグルスイッチとして実装。

### v9 の強化（Anthropic公式プロンプト指針の反映）
ユーザー提示の公式ドキュメント4本（プロンプティングのベストプラクティス／Fable 5／Opus 5／Sonnet 5）を読み、記載手法をツールに実装した。

**構造に関する追加**
- **プロンプト構造の切替を新設**（ステップ3最上部）：「Markdown見出し」／「XMLタグ」。XMLを選ぶと `<role>` `<context>` `<instructions>` `<input>` `<output_format>` `<examples>` `<process>` `<quality_criteria>` `<writing_style>` `<constraints>` で出力される。公式が「指示・素材・例が混在するプロンプトではXMLタグが曖昧さを減らす」と明記しているため。XML時は素材の `"""` 囲みを廃止（タグ自体が区切りになるため二重の囲みが不要）。
- **長文入力モードを新設**：ONにすると素材を先頭・依頼を末尾に配置する。公式に「長文データは指示より前に置く」「クエリを末尾に置くと複数資料で品質が最大30%向上」とあるため。
- **最終指示をモデル階層で分岐**：推奨モデルが Fable/Opus（highティア）のプリセットでは自己検証の指示を出力しない。公式が「Opus 5 / Fable 5 は指示せずとも自己検証するため、検証指示を書くと過剰検証になりトークンと時間だけ増える」と明記しているため。Haiku/Sonnet 向けプリセットでは従来どおり確認指示を残す。

**指示文の書き方の修正**
- **AIっぽさ除去の8ルールを全面書き換え**：「〜しない」の否定形から「〜する＋その理由」の肯定形へ。公式が3本のモデル別ガイドすべてで「肯定的な例は、何をしてはいけないかの指示より効果的」と述べているため。セクション名も「文体の禁止事項」→「文体」に変更。
- **③ 重点レビューの品質基準を修正**：「問題がなければ無理に指摘を作らない」という抑制的な表現を撤回し、「見つけた問題はすべて報告し、確信度と重大度を明記する」に変更。公式に「レビュープロンプトに『重大度の高い問題のみ』『保守的に』と書くと、モデルは指示どおり実バグを黙って落とす」という明確な警告があり、当初の設計が逆効果だったため。

**選択肢・プリセットの追加**
- **思考の手法に3項目追加**：「関連箇所を引用してから答える」（長文で該当箇所に集中させる公式手法）、「徹底的に作り込む」（修飾語で出力を引き上げる手法）、「スコープを広げない」（Opus 5 のタスク範囲拡大を抑える公式スニペット相当）。
- **🎨 UI・デザインプリセットを新設**（Fable/Opus・工数中）：公式の `frontend_aesthetics` スニペットを品質基準に反映。使い古されたフォント（Inter・Roboto・Arial）、白/黒地＋紫グラデーション、定型レイアウトの回避、配色を決め切る、背景に奥行きを作る、動きを要所に集中、実装前に方向性を3〜4案提示して選ばせる。
- **コードプリセットを強化**：「開いていないコードを推測で答えない」（ハルシネーション対策）、「要求されていない抽象化・防御的コードを足さない／検証は外部との境界だけ」（過剰設計対策）、「テストを通すための特殊対応やハードコードをしない」を追加。
- **モデル推奨ストリップに effort 設定を併記**：highティア→`high〜xhigh`、midティア→`high`、lowティア→`low〜medium`。
- **安定化オプションに注記**：「最後に自己チェック」は Fable 5 / Opus 5 では外すのが公式推奨である旨を表示。
- **背景・前提欄の説明を変更**：「状況を伝えると精度が上がる」→「『何のためか』を書くと精度が上がる」。公式の「リクエストだけでなく理由も伝える」に対応。

**特記事項（この版で起きた問題）**
- UI・デザインプリセットを追加した際、キー名 `design` が開発ワークフローの「① 設計・構造」と重複し、後者に上書きされてプリセットが1つ消えていた。ブラウザ上でプリセット数を数えて検出し、キー名を `frontend` に変更して解決。
- 実装後、ブラウザ上で Markdown/XML 両モードの出力、長文モードでの並び順（`<input>` が先頭・`<instructions>` が末尾）、モデル階層別の最終指示の分岐を実行して確認済み。コンソールエラーなし。

### v8 の追加（作業まとめプリセット）
- **用途プリセット「🗂️ 作業まとめ」を新設**（定番タスクグループ、Haiku/Sonnet・工数小）。会話ログや作業内容を貼り付けると、あとから読んでも経緯と結果がわかるMarkdownドキュメントに整形する依頼文を生成する。
- 品質基準は「作業概要／使ったインプット／アウトプット一覧／特記事項」の4項目構成を強制し、このワークスペース（`00_作業`フォルダ）のCLAUDE.mdが定めるまとめ.mdのルールと同じ型にした。時系列の経過説明ではなく「最終的に何がどう変わったか」を軸にすること、判断に迷った点は特記事項に残すことを明記している。
- **R80ルールトグルを新設**（ステップ3の最上部、紫色のスイッチ）。ONにすると「対象・内容」の直後に専用セクションが挿入され、次の4点が出力プロンプトに強制的に組み込まれる：①2文構成・合計80字以内（1文目40字ほど／2文目40字ほど）、②2文目冒頭は順接・逆接・並列・対比・換言・説明のいずれか一種類の接続詞で始める、③構造は「1文目（前提・事実・主張）＋接続詞＋2文目（結論・理由・展開）」、④序論・本論・結論など複数のR80が必要な場合は単位ごとに独立させてつなげる。ユーザーが提示した学習メソッド資料の仕様をそのままプロンプト化したもの。

## v7 の追加（開発ワークフロー＋推奨モデル表示）
- **用途を2グループに再編**：「📌 定番タスク」（8種）と「🛠️ 開発ワークフロー」（3種）
- **開発ワークフロー3工程のプリセットを新設**（モデル使い分け戦略に対応）
  - ① 設計・構造（Fable/Opus・工数大）：コードを書かず設計判断のみ。選択肢と採用/棄却理由、やり直しコストが高い箇所の明示を品質基準に
  - ② 実装・反復（Haiku/Sonnet・工数小）：確定済み仕様どおりに実装。勝手な機能追加・リファクタ禁止、変更箇所の列挙を品質基準に
  - ③ 重点レビュー（Fable/Opus・工数中）：指定観点に絞った徹底レビュー。範囲外に広げない、重要度順＋修正案つき、問題なければ「問題なし」と明言
- **全用途に推奨モデルを表示**
  - 各カード下に色分けバッジ（紫=Fable/Opus、青=Sonnet、緑=Haiku/Sonnet、灰=内容による）
  - 選択中の用途の下に「推奨モデル・工数目安・理由」のストリップを表示（例：要約→Haiku/Sonnet・工数小「仕様が明確な定型作業」）

## v6 のUI全面リニューアル
- **ステップごとに色分けされたカードUI**：①用途=青／②内容=緑／③調整=紫／④文体=オレンジ。各カードのバッジ色とプレビューのセクション色が対応し、「どの操作がプロンプトのどこに効いたか」が一目で分かる
- **プレビューを整形表示に**：セクションごとに色付きラベル＋左ボーダーのブロックで表示。「🎨整形表示／📄テキスト」タブで生のテキストにも切替可（コピーは常に生テキスト）
- **スライダーを5段階スナップ式に**：目盛りラベル（端的〜フォーマル／最小限〜徹底的）を下に常時表示し、ラベルのクリックでも選択可。現在値はラベルの色でハイライト
- **AIっぽさ除去をトグルスイッチに変更**：チェックボックスからON/OFFスイッチへ。2列グリッドで高さを圧縮
- **選択中の用途にチェックバッジ**、ヘッダーはグラデーション、コピーは「📋 コピーして使う」の大ボタンに

## v5 の強化（生成エンジン刷新）
- **品質基準セクションを新設**：各用途プリセットに固有の成功条件を定義（例：要約→「原文にない情報を足さない」「固有名詞・数値を正確に保持」、コード→「そのまま実行できる完全な状態」「勝手に機能を追加しない」）。「# 品質基準（すべて満たすこと）」として出力される
- **依頼文を目的ベースに書き換え**：「〜してください」だけでなく「目的は〜な状態にすること」と成功状態を伝える形に
- **スライダーを5段階の具体的指示文に変換**：トーン（端的〜フォーマル）・分量（最小限〜徹底的、目安字数つき）が自然文としてプロンプトに入る
- **最終指示を追加**：「出力前に条件・品質基準を満たしているか確認し、前置きなしで成果物から出力を始める」を毎回末尾に付与
- **役割の強化**：「その専門家として判断し、専門家水準の成果物を出す」と判断基準として機能させる文に
- **形式・手法・安定化の指示文を具体化**（例：JSON→「有効なJSONのみ、説明文不要」、反対意見→「最も強い反論を挙げて答えてから結論」）
- **内容が空でもテンプレとして機能**：「（ここに内容を貼り付けてください）」のプレースホルダー入りで出力

## v4 の改善
- **トーン・分量をスライダーに変更**（ボタンから）
  - トーン：端的 ←→ 丁寧（内部的には「端的」「カジュアル」「論理的」「標準」「丁寧」の5段階）
  - 分量：短く ←→ 詳しく（内部的には「短く」「標準」「詳しく」の3段階）
  - リアルタイムで値が変わり、プレビューも即座に更新

## v3 の追加
- **プロンプト強化テクニックを追加**（「さらに細かく設定する」内）
  - 背景・前提、対象読者、お手本の例（few-shot）、禁止事項の入力欄
  - 思考の手法（順を追って考える／複数案を出す／根拠を示す／反対意見も検討する）
  - 出力は # 背景・前提 / # 対象読者 / # お手本の例 などのセクションとして構造化
- **ステップ4「文章を自然にする（AIっぽさ除去）」を新設**
  - AIっぽい文章の特徴8種をチェックリスト化：定型的な前置き／締めの決まり文句／曖昧なぼかし表現／強調の多用／箇条書き頼み／テンプレ構成／冗長・過剰な丁寧さ／絵文字・装飾記号
  - チェックすると「# 文体」セクションに具体的な除去指示が自動追加される
  - 「まとめてON／すべて解除」ボタンで一括切り替え可

## v2 の設計方針
- **3ステップ導線**：①用途を選ぶ → ②内容を入力 → ③微調整（任意）
- **用途プリセット8種**（要約／文章作成／メール／翻訳／コード／アイデア出し／添削／自由）。
  選ぶと役割・トーン・分量・形式・安定化オプション・依頼文が自動セットされ、クリックだけでほぼ完成。
- **微調整はセグメント式ボタン**（プルダウン廃止で直感的に）。細かい設定は折りたたみ内に隔離しUIをすっきり化。
- **出力は Markdown 構造**（# 役割 / # 依頼 / # 対象・内容 / # 出力条件 / # 進め方）でAIが解釈しやすい形に。
- 安定化オプションは短いラベル選択→出力時に具体的な指示文へ自動展開（例：「結論から述べる」→「最初に結論・要点を述べてから詳細を説明する」）。
- クリップボードAPIでコピー、右側にリアルタイムプレビュー。

## 特記事項
- 外部ライブラリ不使用、単一HTMLファイルで完結（サーバー不要）。
- レスポンシブ対応（狭い画面ではプレビューが下に回り込む）。
- 「AIの挙動を毎回一定にしたい」という要望に合わせ、安定化を進め方セクションとして明示的にプロンプトへ組み込む設計。
